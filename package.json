{
  "name": "invoke-loader",
  "version": "0.1.1",
  "description": "Webpack loader, which invokes another loader with given options.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ --out-dir ./lib",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "webpack",
    "loader",
    "resolve",
    "invoke",
    "options"
  ],
  "author": "Morulus <vladimirmorulus@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "loader-utils": "^1.1.0",
    "resolve": "^1.7.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
